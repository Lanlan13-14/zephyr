import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {WasmBridge} from '../public/vendor/wterm-fork/core/index.js';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
test('OSC 8 stores, deduplicates and closes links without growing Cell',async()=>{const b=await WasmBridge.load();b.init(80,24);b.writeString('\x1b]8;;https://example.com/a\x1b\\LINK\x1b]8;;\x1b\\ plain');const link=b.getCell(0,0),plain=b.getCell(0,5);assert.equal(b.getHyperlink(link.linkId),'https://example.com/a');assert.equal(plain.linkId,0);assert.equal(b.cellSize,20);});
test('OSC 8 rejects unsafe schemes at render boundary',()=>{const src=fs.readFileSync(path.join(ROOT,'wterm/packages/@wterm/dom/src/renderer.ts'),'utf8');assert.match(src,/\["http:", "https:", "mailto:"\]/);assert.match(src,/rel="noopener noreferrer"/);assert.doesNotMatch(src,/javascript:/);});
