export const APP_NAME = 'Zephyr One';
/* Shell-side display build. CI stamps this via set-version.py (same full
 * display build as the core: marketing + pre suffix). Never hand-edit the
 * pre suffix here; the stamp is the single source of truth. */
export const APP_VERSION = '0.1.0';
export const APP_PRERELEASE = '';
