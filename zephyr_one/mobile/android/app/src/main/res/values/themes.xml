<?xml version="1.0" encoding="utf-8"?>
<!-- The platform window theme only supplies system-bar behavior. Every app colour, shape and type
     value comes from ZephyrTheme in core-ui at runtime. -->
<resources>
    <style name="Theme.ZephyrOne" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Match Frost light background so the first frame is the dashboard, not a black flash. -->
        <item name="android:windowBackground">@color/zephyr_splash</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
